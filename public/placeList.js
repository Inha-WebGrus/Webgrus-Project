document.getElementById('zzim').addEventListener('onclick', async (e) => {
    e.preventDefault();
   if(){
    const id = ;
    const place_id = e.target.id.value;
    
   }
   else{
    var cookieParser = require('cookie-parser');

    app.use(cookieParser);
    app.get('/', function(req,res){
        res.cookie('key', 'value', {
            maxAge:10000
        });
    });
    }
  });